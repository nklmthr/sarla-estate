[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[start]
cmd = "java -Dspring.profiles.active=prod -jar target/sarla-tea-estates-crm-1.0.0.jar --server.port=$PORT"

